// Side-effect CSS imports inside @ace/design-system. Loaded by Vite at
// compile time and never executed; this declaration just keeps `tsc`
// happy so the workspace's typecheck doesn't reject a *.css module
// reference.

declare module '*.css';
